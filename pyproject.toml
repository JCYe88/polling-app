[project]
name = "polling-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.3",
    "python-dotenv>=1.2.2",
    "upstash-redis>=1.7.0",
    "uvicorn>=0.49.0",
]
